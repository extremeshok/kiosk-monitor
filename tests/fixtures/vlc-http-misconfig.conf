# Fixture: replicates the exact misconfig issue #1's reporter ran
# `--doctor` against in v6.9.2 — both instances are MODE=vlc but URL /
# URL2 point at Frigate's HTML Birdseye page rather than a media stream.
# v6.9.0's _url_looks_like_media_for_vlc heuristic must catch this and
# emit a Config warning for each; v6.9.3's doctor must count them in
# the final summary.
MODE="vlc"
URL="http://192.168.1.194:30059/?Birdseye"
MODE2="vlc"
URL2="http://192.168.1.194:30059/?Birdseye"
GUI_USER=""
